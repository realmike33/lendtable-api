const User = require('../src/models/user')

describe('Auth', () => {
  describe('signin', () => {
    it('creates user', async () => {
      expect(1).toEqual(1)
    })
  })
  describe('signup', () => {
    it('creates user', async () => {
      expect(1).toEqual(1)
    })
  })
})
